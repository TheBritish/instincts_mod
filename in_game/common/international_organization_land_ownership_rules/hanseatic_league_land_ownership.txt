hanseatic_league_land_ownership = {
	# Basic ownership rule allowing IO-controlled land in member ports and adjacent areas
	modifier = {
	}

	can_add_trigger = {
		always = yes
	}
	can_remove_trigger = {
		always = yes
	}

	# root = location, recipient = international organization
	can_add_location_trigger = {
		is_land = yes
		is_passable = yes
	}

	# root = location, recipient = international organization
	can_remove_location_trigger = {
		always = yes
	}

	# root = location, recipient = international organization
	on_added = {
	}

	# root = location, recipient = international organization
	on_removed = {
	}

	# root = location, recipient = international organization
	ai_desire_to_add = {
	}

	allow_control_propagation = yes
}
